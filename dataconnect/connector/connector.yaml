# Firebase Data Connect connector configuration
# Generates TypeScript SDK for the frontend

connectorId: "eventease-connector"

generate:
  javascriptSdk:
    outputDir: "../../services/dataconnect-sdk"
    package: "@eventease/dataconnect"

# Authentication configuration
# USER level requires Firebase Auth token
# PUBLIC level allows unauthenticated access
auth:
  defaultLevel: USER
